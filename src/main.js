
/**
 * Class RiscoJS
 * 
 * Main class for RiscoJS web map client
 * 
 * @param {string} p_config_var - Name of  variable containing configuration JSON dictionary
 * @param {string} p_paneldiv_id 	- Id of HTML DIV element to act as RiscoJS map panel
 * @returns {string}
 * 
 */
export class RiscoJS {

	constructor(p_config_var, p_paneldiv_id) {

		console.assert(p_config_var != null, "Class RiscoJS, config_var nula");
		console.assert(p_paneldiv_id != null, "Class RiscoJS, paneldiv_id nula");

		document.getElementById()

	}
}

